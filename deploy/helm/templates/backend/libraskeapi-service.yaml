apiVersion: v1
kind: Service
metadata:
  name: {{ template "libraskeapi-name.fullname" . }}
  labels:
    {{ include "libraskeapi.labels" . | indent 4 }}
spec:
  type: {{ .Values.libraskeapi.service.type }}
  ports:
  - port: {{ .Values.libraskeapi.service.port }}
    name: http
    targetPort: {{ .Values.libraskeapi.service.port }}
    protocol: TCP
  selector:
    {{ include "libraskeapi.labels" . | indent 4 }}
